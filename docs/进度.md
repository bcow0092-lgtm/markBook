# 项目进度

> 这份文件只记录**当前进展与续接点**，不重复设计内容。
> 范围与边界见 [产品设计文档.md](../产品设计文档.md)，技术细节见 [技术方案文档.md](../技术方案文档.md)。

**最后更新：2026-09-19**

---

## 当前状态

| 项 | 值 |
|---|---|
| 当前里程碑 | M1 已完成，M2 未开始 |
| 分支 | `main` |
| 工作区 | 干净 |
| 测试 | 48 个用例，`npm run verify` 全绿 |
| 远端 | `github.com/bcow0092-lgtm/markBook`，**本地领先 13 个提交，尚未推送** |

## 已完成

**M1 骨架 + 风险验证**（2026-09-19）。产出：

| 内容 | 位置 |
|---|---|
| electron-vite 工程与 `verify` 管线 | `package.json` / `electron.vite.config.ts` |
| 安全基线、CSP、单实例锁 | `src/main/index.ts` / `src/renderer/index.html` |
| `app://` 协议与路径穿越防护 | `src/main/protocol.ts` / `src/main/services/appPath.ts` |
| 共享类型与 IPC 契约 | `src/shared/types.ts` |
| 数据层：paths / repository / bookFiles | `src/main/services/` |
| 48 个用例 | `tests/unit/` |
| 6 个样本 EPUB 与生成器 | `tests/fixtures/` |

**三项风险验证的完整结论在技术方案 §7.1**，M2 开工前必读。要点：

1. TXT 多列分页方案**成立**，不需要退路
2. epub.js 渲染**成立**，但依赖三处特定配置：`app://` 的 `corsEnabled` 特权、CSP 的 `style-src blob:`、**不能设 `base-uri`**
3. `app://` 不支持 Range，但原本的担忧基于错误模型，不影响方案

## 下一步：M2 书架

内容：导入流程（staging / 提交 / 回滚）、EPUB 元数据与封面提取、书架列表、删除。

**开工前的三件事：**

1. 读技术方案 §7.1 与 §5.6
2. 写 M2 实施计划，放 `docs/superpowers/plans/`
3. 决定 `main` 要不要推到远端

**M2 必须处理的一个已知坑**（技术方案 §7.1 末尾）：损坏的 OPF 会让 epub.js **挂起**——`openFailed` 事件正常触发，但 `rendition.display()` 既不 resolve 也不 reject。导入流程**必须自带超时**，否则导入会卡死且 `.staging/` 里的残留不会被 `abortImport` 清掉。

## 环境注意事项

这台机器上跑这个项目，有三处不照做就会失败：

- **`.npmrc` 里的 `ELECTRON_MIRROR`** —— Electron 二进制从 GitHub Release 直连实测 15 分钟无进展，必须走 npmmirror
- **`package.json` 里的 `allowScripts`** —— npm 11 默认拦 postinstall，缺这条 Electron 二进制不会下载
- **Node 24** —— 技术方案 §6.5 原写 20，但 20 已于 2026-04 EOL

## 开发时可用的两样东西

- **渲染进程 console 转发**：开发模式下主进程会把渲染进程的 console 打到终端 stdout（`src/main/index.ts`）。CSP 违规、React 报错这类只出现在 DevTools 里的信息，不转发在终端完全看不见
- **风险验证页**：`src/renderer/src/dev/VerifyPage.tsx`，需要重跑 M1 的三项验证时挂到 `App.tsx` 即可，它会往 console 打 `[VERIFY:*]` 日志
