# 马克阅读器 · 技术方案文档（MVP）

> 配套文档：[产品设计文档.md](./产品设计文档.md)
> 本文档定义 MVP 的技术选型、架构设计与实施路径。

---

## 1. 技术选型总览

| 层次 | 选型 | 说明 |
|---|---|---|
| 桌面运行时 | **Electron**（当前稳定版） | 内嵌 Chromium，EPUB 渲染有成熟方案 |
| 语言 | **TypeScript 5** | 主进程 / 渲染进程 / 共享类型统一 |
| UI 框架 | **React 18** | |
| 构建工具 | **electron-vite** | 主/预加载/渲染三端统一 Vite 构建，HMR 体验好 |
| 打包 | **electron-builder** | 输出 NSIS 安装包 |
| 状态管理 | **Zustand** | 体量小，无需 Redux |
| 样式 | **Tailwind CSS** + **Radix UI** | 阅读器需要大量自定义样式，Tailwind 可控性更好 |
| EPUB 渲染 | **epub.js** | 最成熟的浏览器端 EPUB 引擎 |
| TXT 处理 | **自研** | 编码检测 + 章节切分，无成熟库可用 |
| 编码转换 | **jschardet** + **iconv-lite** | 中文 TXT 大量为 GBK/GB18030 |
| 持久化 | **JSON 文件**（主进程内） | 见 §4，MVP 不引入 SQLite |
| 单元测试 | **Vitest** + **React Testing Library** | |
| 端到端测试 | **Playwright**（Electron 支持） | |

### 1.1 为什么是 Electron 而不是 Tauri

EPUB 的本质是 zip 包内的 XHTML + CSS，**任何实现都必须有浏览器引擎做排版渲染**。可选范围因此只有「内嵌 Chromium」和「复用系统 WebView」两条路。

选择 Electron 的理由：

- EPUB 渲染交给 Chromium，是与 epub.js 被验证过无数次的组合；Tauri 走系统 WebView2，版本差异带来的踩坑概率明显更高
- MVP 阶段开发速度优先，Node.js 生态处理文件 IO / 编码转换更顺手
- 后续若体积、内存成为硬指标，可迁移到 Tauri，前端代码几乎可原样复用

**放弃 Electron 的唯一理由**是安装包体积（~100MB）与内存占用（~300MB）不可接受。本项目定位为个人本地工具，可接受。

### 1.2 明确不采用的技术

| 方案 | 不采用的原因 |
|---|---|
| WPF / WinUI 3 | 原生渲染 EPUB 需要自研 CSS 引擎，最终仍要嵌 WebView2，白白多一层 |
| Qt / Flutter 自绘 | 同上，且生态内没有能打的 EPUB 渲染库 |
| better-sqlite3 | native 模块需针对 Electron ABI 重编译，Windows 上依赖 VS Build Tools，抬高环境门槛；MVP 数据量也远未到需要 SQLite |

---

## 2. 整体架构

### 2.1 进程职责划分

```
┌─────────────────────────────────────────────────────┐
│  主进程 (Node.js)                                    │
│  · 文件系统：导入 / 删除 / 复制书籍文件               │
│  · 持久化：书架数据、阅读进度、阅读设置的读写          │
│  · 系统对话框：文件选择、确认删除                     │
│  · 自定义协议：app:// 暴露书籍文件给渲染进程           │
│  · 窗口生命周期、退出前落盘                           │
└───────────────┬─────────────────────────────────────┘
                │ IPC (ipcMain / ipcRenderer)
┌───────────────┴─────────────────────────────────────┐
│  Preload                                             │
│  · contextBridge 暴露白名单 API                       │
│  · 不暴露任何原始 ipcRenderer / fs / path             │
└───────────────┬─────────────────────────────────────┘
                │ window.api.*
┌───────────────┴─────────────────────────────────────┐
│  渲染进程 (React)                                     │
│  · 书架页、阅读页、目录面板、设置面板                   │
│  · epub.js 在此层运行（依赖 DOM）                     │
│  · TXT 分页渲染、进度计算                             │
└─────────────────────────────────────────────────────┘
```

**关键决策：元数据解析放在渲染进程。** 导入 EPUB 时不需要在主进程引入 zip/xml 解析库，直接用渲染进程已有的 epub.js 打开一次即可拿到 `book.loaded.metadata` 与 `book.coverUrl()`。相比手写 `container.xml → OPF` 解析，代码更少，且能兼容各种畸形 EPUB。

### 2.2 安全基线

Electron 的安全默认值必须显式收紧：

```ts
new BrowserWindow({
  webPreferences: {
    contextIsolation: true,     // 必须
    nodeIntegration: false,     // 必须
    sandbox: true,
    preload: path.join(__dirname, '../preload/index.js'),
  },
})
```

- 渲染进程**不得**直接接触 `fs` / `path`，一切文件操作走 IPC，路径由主进程校验
- 自定义协议 `app://` 的 handler 内必须做路径规范化校验，拒绝越出 `books/` 目录的请求（防路径穿越）
- 渲染进程页面加载时设置 CSP

### 2.3 单实例约束（必须实现）

```ts
if (!app.requestSingleInstanceLock()) {
  app.quit()
} else {
  app.on('second-instance', () => {
    // 已有实例：聚焦并还原窗口，而不是开新窗口
    if (mainWindow) {
      if (mainWindow.isMinimized()) mainWindow.restore()
      mainWindow.focus()
    }
  })
}
```

**这不是体验优化，是数据正确性的前提。** 本方案采用「内存持有全量数据 + 防抖落盘」（§4.3），若允许两个实例并存，两个进程会各自持有 `library.json` 的独立副本并互相覆盖，导致书架数据损坏。Windows 用户双击两次任务栏图标即可触发，属于必现问题。

---

## 3. 目录结构

```
马克阅读器/
├── electron.vite.config.ts
├── electron-builder.yml
├── package.json
├── src/
│   ├── shared/
│   │   └── types.ts              # 主/渲染共享的类型定义与 IPC 通道常量
│   ├── main/
│   │   ├── index.ts              # 应用入口、窗口创建
│   │   ├── protocol.ts           # app:// 协议注册
│   │   ├── ipc/
│   │   │   ├── library.ts        # books:import / books:delete / books:list
│   │   │   └── reader.ts         # progress:get / progress:save / settings:*
│   │   └── services/
│   │       ├── repository.ts     # 持久化读写（JSON），对外暴露仓储接口
│   │       ├── bookFiles.ts      # 书籍文件复制、删除、路径校验
│   │       └── paths.ts          # appData 目录常量
│   ├── preload/
│   │   └── index.ts              # contextBridge 白名单 API
│   └── renderer/
│       ├── index.html
│       └── src/
│           ├── main.tsx
│           ├── App.tsx           # 极简路由：书架 ↔ 阅读器
│           ├── pages/
│           │   ├── Shelf/        # 书架页
│           │   └── Reader/       # 阅读页
│           ├── components/
│           │   ├── ReaderView/   # epub.js 封装（含分页、翻页、位置恢复）
│           │   ├── TxtView/      # TXT 分页渲染
│           │   ├── TocPanel/     # 目录抽屉
│           │   ├── ProgressBar/  # 进度条
│           │   └── SettingsPanel/# 字体 / 行距 / 页宽
│           ├── store/
│           │   ├── library.ts    # Zustand：书架数据
│           │   └── reader.ts     # Zustand：当前阅读状态与设置
│           └── lib/
│               ├── txt/
│               │   ├── encoding.ts   # 编码检测与转换
│               │   └── chapters.ts   # 章节切分
│               └── progress.ts       # 进度计算与防抖落盘
└── tests/
    ├── unit/
    └── e2e/
```

路由说明：MVP 只有「书架」和「阅读器」两个视图，用 Zustand 里的一个 `currentView` 状态切换即可，不引入 react-router。

---

## 4. 数据设计

### 4.1 存储位置

```
%APPDATA%/马克阅读器/
├── library.json          # 书架元数据 + 阅读设置
├── books/                # 书籍文件副本
│   ├── {uuid}.epub
│   └── {uuid}.txt
└── covers/               # 提取出的封面图
    └── {uuid}.png
```

导入时**复制**文件到 `books/` 而非引用原路径，避免用户移动/删除源文件导致书籍失效。

### 4.2 数据模型

```ts
type BookFormat = 'epub' | 'txt'

interface ReadingProgress {
  /** EPUB 用 CFI 字符串；TXT 用字符偏移量 */
  location: string | number
  /** 0 ~ 1 */
  percentage: number
  chapterIndex: number
  chapterTitle: string
}

interface Book {
  id: string                 // uuid
  title: string
  author: string
  format: BookFormat
  fileName: string           // books/ 下的文件名
  coverFileName: string | null
  size: number               // bytes
  addedAt: number
  lastReadAt: number | null
  progress: ReadingProgress | null
}

interface ReaderSettings {
  fontSize: number           // px
  lineHeight: number         // 倍数
  pageWidth: number          // px
}

interface LibraryData {
  version: 1                 // 数据结构版本，供未来迁移
  books: Book[]
  settings: ReaderSettings
}
```

### 4.3 为什么 MVP 用 JSON 而不是 SQLite

书架数据量级是几十到几百本书，`library.json` 全量读写耗时在毫秒级，完全够用。引入 SQLite 会带来 native 模块编译负担，收益为零。

**但要做隔离**：所有持久化访问收敛到 `main/services/repository.ts`，对外暴露的是 `listBooks()` / `upsertBook()` / `getSettings()` 这样的接口，不泄漏"底层是 JSON"这个事实。等到后续迭代引入书签、笔记、高亮（数据条数会到万级、且需要按书检索）时，只替换 repository 的实现，上层不受影响。

写入策略：内存持有全量数据，变更后**防抖 500ms** 落盘；应用退出前强制 flush 一次。

---

## 5. 核心技术方案

### 5.1 书籍文件如何到达渲染进程

epub.js 需要一个 URL 或 ArrayBuffer 来加载书。渲染进程不能直接读文件，因此**注册自定义协议**：

```ts
// 应用启动前
protocol.registerSchemesAsPrivileged([
  { scheme: 'app', privileges: { standard: true, secure: true, supportFetchAPI: true, stream: true } },
])

// app ready 后
protocol.handle('app', (request) => {
  const rel = new URL(request.url).pathname          // 如 /books/xxx.epub
  const abs = path.resolve(BOOKS_DIR, '.' + rel)
  if (!abs.startsWith(BOOKS_DIR)) return new Response(null, { status: 403 })  // 防穿越
  return net.fetch(pathToFileURL(abs).toString())    // 支持 Range 请求
})
```

渲染进程即可：

```ts
const book = ePub('app://local/books/xxx.epub')
```

选这个方案而不是「主进程读成 Buffer 再经 IPC 传给渲染进程」，有两个原因：

1. **支持 Range 请求**（`stream: true`）。大 EPUB 内部有图片，epub.js 会按需读取，一次性传整个 ArrayBuffer 会有明显内存峰值。
2. **不污染 IPC**。几百 MB 的二进制走 IPC 序列化既不优雅也慢。

### 5.2 EPUB 分页渲染

```ts
const rendition = book.renderTo(containerEl, {
  flow: 'paginated',      // 分页而非滚动 —— 产品的核心交互
  spread: 'none',         // 单栏，因为「页面宽度」需要可调
  width: '100%',
  height: '100%',
})
rendition.display(savedCfi ?? undefined)
```

| 需求 | 实现 |
|---|---|
| 上/下页 | `rendition.prev()` / `rendition.next()` |
| 键盘翻页 | 监听 `keydown`：← / PageUp / ↑ 上一页，→ / PageDown / ↓ / 空格 下一页 |
| 点击翻页 | 容器左右三分之一区域设热区 |
| 当前位置 | `rendition.on('relocated', loc => ...)` |
| 全书进度 | `book.locations.percentageFromCfi(loc.start.cfi)` |
| 目录 | `book.loaded.navigation.toc` → `rendition.display(href)` |
| 恢复位置 | `rendition.display(savedCfi)` |

**必须处理的坑：**

1. **`book.locations.generate()` 是耗时操作。** 它要对全书做分词以计算位置索引，长篇小说可能需要数秒。做法：首次打开时后台异步生成，结果缓存进 `library.json`；未生成完成时进度条退化为「按 spine 章节序号估算」，生成完毕后再切换到精确值。不要阻塞首屏。

2. **改设置会丢位置。** 调整字号/行距后，原来那一页的内容会重排，`rendition` 内部位置会漂移。正确流程是：**改之前记录当前 CFI → 应用新样式 → `rendition.display(cfi)` 恢复到同一处**。少了这一步，用户每调一次字号就会"跳页"。

3. **图片、表格的跨页表现**是 epub.js 分页模式最常见的边界问题。需要专门的测试书目覆盖（含大图、宽表格、代码块的 EPUB），这是本项目最大的技术不确定性。

### 5.3 阅读设置生效方式

字号、行距通过 epub.js 的主题系统直接注入，不修改原书 CSS：

```ts
rendition.themes.fontSize(`${settings.fontSize}px`)
rendition.themes.override('line-height', String(settings.lineHeight))
```

「页面宽度」的语义**对齐微信读书的「页边距」**：内容区居中，调整左右内边距，视觉结果是每行字数变化。提供窄 / 适中 / 宽三档。

单栏居中布局下，「改列宽」与「改页边距」是等价的（列变窄即留白变宽），因此实现上就是给内容容器加左右 padding：

```ts
const PAGE_MARGIN_PRESETS = { narrow: 24, medium: 56, wide: 96 }  // px
rendition.themes.override('padding-left',  `${px}px`)
rendition.themes.override('padding-right', `${px}px`)
rendition.resize()   // 触发重新分页
```

> 注意：与字号、行距一样，`resize()` 会导致位置漂移，必须走 §5.2 的「存 CFI → 应用 → 恢复」。三档间距变化幅度大，这一步不能省。

### 5.4 TXT 处理

TXT 没有 CSS，需要自己造排版。分三步：

**（1）编码检测与转换**

中文 TXT 大量是 GBK / GB18030，直接按 UTF-8 读会乱码。主进程读文件为 Buffer 后用 `jschardet` 探测编码，再经 `iconv-lite` 转成 UTF-8，最后交给渲染进程。

**（2）章节切分**

按正则匹配章节标题行，常见格式需全部覆盖：

```
第[一二三四五六七八九十百千零〇\d]+[章节回卷篇]
Chapter\s+\d+
^\d+[\.、]\s*    // 纯数字编号
```

匹配不到任何章节时，降级为「按固定字符数（如 5000 字）等分」成一页一章，保证目录和进度条始终可用。

**（3）分页渲染**

不要在 JS 里逐段测量高度累加（慢且易错）。改用 **CSS 多列布局**，让 Chromium 自己做分页：

```css
.txt-content {
  column-width: {pageWidth}px;
  column-gap: 40px;
  height: 100%;
}
```

横向平移容器即完成翻页。好处是分页逻辑与 EPUB 侧思路一致，字号/行距变化后浏览器自动重排，无需手动重算。

**大文件性能**：几十 MB 的 TXT 若一次性塞进 DOM 会卡死。按章节分批渲染，只挂载当前章节 ±1 的内容。

### 5.5 进度持久化时机

`relocated` 事件在翻页时高频触发，不能每次都写盘。

- 内存中实时更新 Zustand store（进度条要跟手）
- 落盘做 **1 秒防抖**
- 触发立即落盘的时机：切换章节、退出阅读页、窗口关闭（`before-quit` 中同步 flush）

### 5.6 导入流程

```
渲染进程                          主进程
   │ 调用 api.importBook()
   │ ──────────────────────────────▶
   │                          弹出文件选择对话框
   │                          校验扩展名 (.epub/.txt)
   │                          生成 uuid，复制到 books/
   │  ◀────────────────────────  返回 { id, format, fileName }
   │
   ├─ EPUB: ePub('app://local/books/{id}.epub')
   │        → 读 metadata(title/author)、coverUrl()
   │        → coverUrl 转 base64 或下载到 covers/
   ├─ TXT:  读文件 → 编码检测 → 章节切分 → 取书名（文件名）/ 首行标题
   │
   │ 调用 api.saveBook(book)
   │ ──────────────────────────────▶  写入 library.json
   ▼
刷书架列表
```

---

## 6. 测试策略

> 项目规范要求：每次改动后必须编写或更新测试，交付前全部通过。

### 6.1 单元测试（Vitest）

优先覆盖纯函数，这是性价比最高的部分：

| 模块 | 用例 |
|---|---|
| `lib/txt/chapters.ts` | 各种章节标题格式的切分；无章节时的降级；空文件 |
| `lib/txt/encoding.ts` | UTF-8 / GBK / GB18030 样本文件的正确解码 |
| `lib/progress.ts` | 百分比计算、防抖落盘的行为 |
| `main/services/repository.ts` | 增删改查、并发写入、损坏 JSON 的容错、版本迁移 |
| `main/services/bookFiles.ts` | **路径穿越防护**（`../../` 必须被拒绝） |

### 6.2 组件测试（Vitest + RTL）

书架列表渲染、设置面板交互、目录面板跳转回调。

### 6.3 端到端测试（Playwright + Electron）

覆盖 MVP 主链路：

1. 导入一本 EPUB → 书架出现该书 → 封面/书名/作者正确
2. 打开 → 翻三页 → 退出 → 重新打开 → **位置恢复**
3. 导入 TXT（GBK 编码样本）→ 打开 → 中文不乱码
4. 调整字号 → 位置不跳变
5. 删除书籍 → 书架移除，`books/` 下文件已清除

### 6.4 必须人工验证的部分

epub.js 的分页是**视觉行为**，E2E 断言难以覆盖。以下需要人工书目清单验证，建议在 `tests/fixtures/` 放置覆盖以下特征的样本 EPUB：

- 大量内嵌图片
- 超宽表格（必然溢出单页）
- 代码块、引用块
- 中英混排
- 无封面 / 无作者元数据
- 损坏的 OPF（容错性）

### 6.5 CI 与自动化执行

主仓库托管在 **GitHub**，CI 使用 **GitHub Actions**。若后续国内拉取变慢，再补一个 Gitee 镜像仓库，但不作为主仓库。

**本地脚本**（CI 与本地共用同一入口，保证行为一致）：

```json
// package.json
"scripts": {
  "lint":      "eslint .",
  "typecheck": "tsc --noEmit",
  "test":      "vitest run",
  "test:e2e":  "playwright test",
  "verify":    "npm run lint && npm run typecheck && npm run test"
}
```

- **`npm run verify`** 是「交付前验证」的单一入口，覆盖静态检查 + 单元/组件测试。
- **`npm run test:e2e`** 单独执行。E2E 已在 §6.3 收敛至 2 条，但仍需启动 Electron，耗时较长。

**工作流**（`.github/workflows/ci.yml`）：

```yaml
runs-on: windows-latest     # 本项目是 Windows 桌面应用，
                            # Linux runner 验证不了打包与路径行为
steps:
  - checkout
  - setup-node 20 (cache: npm)
  - npm ci
  - npm run verify
```

触发策略：

| 事件 | 执行内容 |
|---|---|
| `pull_request` | `npm run verify` |
| `push` 到 `main` | `npm run verify` + `npm run test:e2e` |
| `workflow_dispatch` | 完整套 + 打包产物验证（里程碑节点手动跑） |

日常迭代不因 E2E 拖慢反馈，关键节点仍有覆盖。

> **额度提醒**：私人仓库免费额度 2000 分钟/月，Windows runner 按 **2 倍倍率**计费，实际约 1000 分钟。E2E 单次启动 Electron 约 1–3 分钟，因此把它限制在 push 到 main，而不是每次 PR 都跑。

**pre-push 钩子不再需要**。引入 CI 后，钩子会拖慢每次推送，且「交付前验证」的触发点是我完成改动时（见 CLAUDE.md），而非推送时 —— 由我在交付前执行 `npm run verify` 即可。

---

## 7. 实施路径

| 阶段 | 内容 | 产出 |
|---|---|---|
| **M1 骨架** | electron-vite 工程搭建、IPC 通道与类型定义、安全基线、app:// 协议、repository 层 + 测试 | 能启动的空窗口，数据层可读写且有测试 |
| **M2 书架** | 导入流程（含文件复制、EPUB 元数据/封面提取）、书架列表、删除 | 能导入并展示书，重启后仍在 |
| **M3 EPUB 阅读** | 分页渲染、翻页、目录、进度条、位置恢复 | **主链路打通（最大风险已释放）** |
| **M4 TXT 阅读** | 编码检测、章节切分、多列分页、大文件分段渲染 | 两种格式均可用 |
| **M5 设置与打磨** | 字号 / 行距 / 页宽、位置保持、空态与异常态 | MVP 功能完整 |
| **M6 打包** | electron-builder 配置、NSIS 安装包、图标 | 可交付的 Windows 安装包 |

**M3 排在最前面验证**，因为它承载了本项目最大的技术不确定性。若 epub.js 的分页在真实书籍上表现不达预期，越早发现，调整成本越低。

---

## 8. 风险清单

| 风险 | 等级 | 应对 |
|---|---|---|
| epub.js 分页在图片/表格上的边界问题 | **高** | M3 优先验证；准备多种样本 EPUB；必要时对特定元素做 CSS 兜底 |
| `locations.generate()` 长书耗时数秒 | 中 | 后台异步 + 缓存 + 未就绪时降级估算 |
| 改设置导致阅读位置跳变 | 中 | 改前存 CFI，改后 `display(cfi)` 恢复 |
| TXT 编码识别错误导致乱码 | 中 | `jschardet` + 用户可手动指定编码的兜底入口 |
| 超大 TXT（>30MB）渲染卡顿 | 中 | 按章节分段挂载，只保留当前 ±1 章 |
| 打包后 `app://` 协议路径解析差异 | 中 | 用 `path.resolve` 而非字符串拼接；打包后做一次完整回归 |
| 用户移动源文件导致书籍失效 | 低 | 导入时复制副本到 `books/` |

---

## 9. 依赖清单

**运行时**

```
electron
epubjs
jschardet
iconv-lite
zustand
react / react-dom
```

**开发时**

```
electron-vite
electron-builder
typescript
vite
@vitejs/plugin-react
tailwindcss
@radix-ui/react-*      # 按需引入
vitest
@testing-library/react
@playwright/test
eslint / prettier
```

依赖刻意保持精简。任何新增依赖前先问：标准库或已有依赖能否覆盖。

---

## 10. 已确认的产品决策

1. **「页面宽度」对齐微信读书的「页边距」** —— 内容区居中 + 左右留白三档调节，详见 §5.3。
2. **不做深色 / 护眼主题** —— MVP 不引入主题切换，阅读区固定浅色背景。若后续需要，EPUB 侧需在主题系统覆盖 `background` 与 `color`，TXT 侧成本很低。
3. **TXT 无规范章节标题时走降级方案** —— 按固定字数切分，接受目录质量下降。不做用户手动指定章节规则的功能。
