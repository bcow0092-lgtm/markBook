# 马克阅读器 · 技术方案文档（MVP）

> 配套文档：[产品设计文档.md](./产品设计文档.md)
> 本文档定义 MVP 的技术选型、架构设计与实施路径。

---

## 1. 技术选型总览

| 层次 | 选型 | 说明 |
|---|---|---|
| 桌面运行时 | **Electron**（当前稳定版） | 内嵌 Chromium，EPUB 渲染有成熟方案 |
| 语言 | **TypeScript 6** | 主进程 / 渲染进程 / 共享类型统一 |
| UI 框架 | **React 19** | |
| 构建工具 | **electron-vite** | 主/预加载/渲染三端统一 Vite 构建，HMR 体验好 |
| 打包 | **electron-builder** | 输出 NSIS 安装包 |
| 状态管理 | **Zustand** | 体量小，无需 Redux |
| 样式 | **Tailwind CSS** + **Radix UI** | 阅读器需要大量自定义样式，Tailwind 可控性更好 |
| EPUB 渲染 | **epubjs 0.3.93**（锁定版本） | 见 §1.3 |
| TXT 处理 | **自研** | 编码检测 + 章节切分，无成熟库可用 |
| 编码转换 | **jschardet** + **iconv-lite** | 中文 TXT 大量为 GBK/GB18030 |
| 持久化 | **JSON 文件**（主进程内） | 见 §4，MVP 不引入 SQLite |
| 单元测试 | **Vitest** + **React Testing Library** | |
| 端到端测试 | **Playwright**（Electron 支持） | |

### 1.1 为什么是 Electron 而不是 Tauri

EPUB 的本质是 zip 包内的 XHTML + CSS，**任何实现都必须有浏览器引擎做排版渲染**。可选范围因此只有「内嵌 Chromium」和「复用系统 WebView」两条路。

选择 Electron 的理由：

- EPUB 渲染交给 Chromium，是与 epub.js 被验证过无数次的组合；Tauri 走系统 WebView2，版本差异带来的踩坑概率明显更高
- MVP 阶段开发速度优先，Node.js 生态处理文件 IO / 编码转换更顺手
- 后续若体积、内存成为硬指标，可迁移到 Tauri，前端代码几乎可原样复用

**放弃 Electron 的唯一理由**是安装包体积（~100MB）与内存占用（~300MB）不可接受。本项目定位为个人本地工具，可接受。

### 1.2 明确不采用的技术

| 方案 | 不采用的原因 |
|---|---|
| WPF / WinUI 3 | 原生渲染 EPUB 需要自研 CSS 引擎，最终仍要嵌 WebView2，白白多一层 |
| Qt / Flutter 自绘 | 同上，且生态内没有能打的 EPUB 渲染库 |
| better-sqlite3 | native 模块需针对 Electron ABI 重编译，Windows 上依赖 VS Build Tools，抬高环境门槛；MVP 数据量也远未到需要 SQLite |

### 1.3 epubjs 的版本策略

`epubjs` 自 0.3.93（2022-05）之后未再发布新版本。这个选型仍然成立——它是浏览器端被验证最多的 EPUB 渲染引擎，本项目依赖的分页、CFI、位置索引能力都已稳定多年。

停更的实际含义是：遇到必须修的上游 bug 时无人修。应对方式是用 `patch-package` 打局部补丁，补丁文件一并提交进仓库。**不 fork、不换库**——Readium 的 TS 工具包虽然维护活跃，但定位偏 Web / 移动阅读器，Electron 集成资料稀少，分页与位置计算要自己接，风险和工作量都明显更大。

`package.json` 中锁定精确版本 `"epubjs": "0.3.93"`，不加 `^`。

---

## 2. 整体架构

### 2.1 进程职责划分

```
┌─────────────────────────────────────────────────────┐
│  主进程 (Node.js)                                    │
│  · 文件系统：导入 / 提交 / 回滚 / 删除书籍文件         │
│  · 持久化：书架数据、阅读进度、阅读设置的读写          │
│  · TXT：编码检测、转码、章节切分                      │
│  · 系统对话框：文件选择、确认删除                     │
│  · 自定义协议：app:// 暴露书籍文件给渲染进程           │
│  · 窗口生命周期、退出前落盘                           │
└───────────────┬─────────────────────────────────────┘
                │ IPC (ipcMain / ipcRenderer)
┌───────────────┴─────────────────────────────────────┐
│  Preload                                             │
│  · contextBridge 暴露白名单 API                       │
│  · 不暴露任何原始 ipcRenderer / fs / path             │
└───────────────┬─────────────────────────────────────┘
                │ window.api.*
┌───────────────┴─────────────────────────────────────┐
│  渲染进程 (React)                                     │
│  · 书架页、阅读页、目录面板、设置面板                   │
│  · epub.js 在此层运行（依赖 DOM）                     │
│  · TXT 分页渲染、进度计算                             │
└─────────────────────────────────────────────────────┘
```

**关键决策：EPUB 元数据解析放在渲染进程，TXT 编码与切分放在主进程。**

导入 EPUB 时不需要在主进程引入 zip/xml 解析库，直接用渲染进程已有的 epub.js 打开一次即可拿到 `book.loaded.metadata` 与 `book.coverUrl()`。相比手写 `container.xml → OPF` 解析，代码更少，且能兼容各种畸形 EPUB。

TXT 反过来——编码检测必须读到原始字节（只有主进程能读文件），章节切分又需要全文。把转码与切分都留在主进程，渲染进程既不需要经 IPC 接收大字符串，也不需要重复做这个确定性计算。

### 2.2 安全基线

Electron 的安全默认值必须显式收紧：

```ts
new BrowserWindow({
  webPreferences: {
    contextIsolation: true,     // 必须
    nodeIntegration: false,     // 必须
    sandbox: true,
    preload: path.join(__dirname, '../preload/index.js'),
  },
})
```

我们的 preload 只使用 `contextBridge` 与 `ipcRenderer`，两者在 `sandbox: true` 下均可用，因此不需要为沙箱放开任何东西。

- 渲染进程**不得**直接接触 `fs` / `path`，一切文件操作走 IPC，路径由主进程校验
- 自定义协议 `app://` 的 handler 内必须做路径规范化校验，拒绝越出白名单目录的请求（见 §5.1）
- 渲染进程页面加载时设置 CSP：

```html
default-src 'self';
script-src 'self';
style-src 'self' 'unsafe-inline' blob:;
img-src 'self' app: data: blob:;
font-src 'self' app: data:;
frame-src 'self' app: blob:;
connect-src 'self' app: blob:;
object-src 'none';
```

每一条放宽都有明确来源，不是照抄模板：

| 指令 | 为什么需要 |
|---|---|
| `style-src 'unsafe-inline'` | epub.js 需向书籍 iframe 注入内联样式 |
| `style-src blob:` | epub.js 用 blob URL 挂载书籍样式表。漏掉这条样式表会被静默拦掉（M1 实测） |
| `frame-src app: blob:` | epub.js 把书渲染进 iframe，不放行则书完全不显示 |
| `img-src blob:` | 书架上要显示 `app://` 下已落盘的封面图 |
| `connect-src blob:` | 封面提取要 `fetch(book.coverUrl())`，而它返回的是 blob URL。**M2 实测补出来的**：M1 只调了 `coverUrl()` 拿地址、没有真去 fetch，所以没暴露；少这一条封面会静默丢失 |
| `img-src app:` / `font-src app:` | 书籍内的图片、字体经 `app://` 加载 |

**刻意不设置 `base-uri`。** 初版写了 `base-uri 'none'`，M1 实测发现 epub.js 要靠设置 document 的 base URI 来解析书内相对路径，加了这条书就读不出来。

`script-src` 保持 `'self'`——书里的脚本一律不执行。

> **关键机制：epub.js 把书渲染进 blob/srcdoc iframe，而 iframe 会继承父页面的 CSP。** 所以上面每一条都会同样施加到**书籍内容**上，不只是渲染页本身。这是本方案最容易踩错的地方：`script-src` 的收紧因此更值钱（书里的脚本确实执行不了），但 `style-src` / `base-uri` 这类就不能按普通网页的严格程度来设。

**CSP 用 meta 标签而非主进程下发响应头。** 响应头是 session 级的，会把 `script-src 'self'` 一并施加到 app:// 响应的书籍资源上，与 `frame-src` / `img-src` 需要放宽的场景自相矛盾；meta 的作用域恰好只覆盖渲染页。

### 2.3 单实例约束（必须实现）

```ts
if (!app.requestSingleInstanceLock()) {
  app.quit()
} else {
  app.on('second-instance', () => {
    // 已有实例：聚焦并还原窗口，而不是开新窗口
    if (mainWindow) {
      if (mainWindow.isMinimized()) mainWindow.restore()
      mainWindow.focus()
    }
  })
}
```

**这不是体验优化，是数据正确性的前提。** 本方案采用「内存持有全量数据 + 防抖落盘」（§4.3），若允许两个实例并存，两个进程会各自持有 `library.json` 的独立副本并互相覆盖，导致书架数据损坏。Windows 用户双击两次任务栏图标即可触发，属于必现问题。

---

## 3. 目录结构

```
马克阅读器/
├── electron.vite.config.ts
├── electron-builder.yml
├── package.json
├── src/
│   ├── shared/
│   │   └── types.ts              # 主/渲染共享的类型定义与 IPC 通道常量
│   ├── main/
│   │   ├── index.ts              # 应用入口、窗口创建、单实例锁
│   │   ├── protocol.ts           # app:// 协议注册与白名单校验
│   │   ├── ipc/
│   │   │   ├── library.ts        # books:import / commit / abort / delete / list
│   │   │   └── reader.ts         # locations:get / locations:save / progress:save / settings:*
│   │   └── services/
│   │       ├── repository.ts     # 持久化读写（JSON），对外暴露仓储接口
│   │       ├── bookFiles.ts      # 文件复制、提交、回滚、删除、路径校验
│   │       ├── paths.ts          # appData 目录常量
│   │       └── txt/
│   │           ├── encoding.ts   # 编码检测与转换（jschardet + iconv-lite）
│   │           └── chapters.ts   # 章节切分（纯函数）
│   ├── preload/
│   │   └── index.ts              # contextBridge 白名单 API
│   └── renderer/
│       ├── index.html
│       └── src/
│           ├── main.tsx
│           ├── App.tsx           # 极简路由：书架 ↔ 阅读器
│           ├── theme.ts          # 色值集中定义（见 §11）
│           ├── pages/
│           │   ├── Shelf/        # 书架页
│           │   └── Reader/       # 阅读页
│           ├── components/
│           │   ├── reader/
│           │   │   ├── engine.ts      # ReaderEngine 接口（§5.7）
│           │   │   ├── EpubEngine.ts  # epub.js 实现
│           │   │   └── TxtEngine.ts   # 多列分页实现
│           │   ├── TocPanel/     # 目录抽屉
│           │   ├── ProgressBar/  # 进度条
│           │   └── SettingsPanel/# 字号 / 行距 / 页边距
│           ├── store/
│           │   ├── library.ts    # Zustand：书架数据
│           │   └── reader.ts     # Zustand：当前阅读状态与设置
│           └── lib/
│               └── progress.ts   # 进度计算与防抖落盘
└── tests/
    ├── fixtures/                 # 样本 EPUB / TXT（二进制入库，见 §6.4）
    ├── main/                     # 主进程侧测试，跑在 node 环境
    ├── renderer/                 # 渲染进程侧测试，需要 DOM 的在文件顶部加
    │                             # `// @vitest-environment jsdom`
    └── e2e/
```

路由说明：MVP 只有「书架」和「阅读器」两个视图，用 Zustand 里的一个 `currentView` 状态切换即可，不引入 react-router。

---

## 4. 数据设计

### 4.1 存储位置

> **目录名是 `Mark Reader`，不是「马克阅读器」。** 这里原本写的是中文名，但
> `package.json` 一直到 M6 才定下 `productName`。M6 定的是全 ASCII 的
> `Mark Reader` —— Electron 用它作为 `app.getName()` 的结果，也因此决定
> userData 目录名。界面与安装包上的显示名仍是「马克阅读器」，只是落盘路径
> 走 ASCII，避免非 ASCII 路径在各类工具链里的边缘问题。

```
%APPDATA%/Mark Reader/
├── library.json           # 书架元数据 + 阅读设置
├── books/                 # 书籍文件
│   ├── {uuid}.epub
│   ├── {uuid}.txt         # TXT 原始字节，原样保留
│   └── .staging/          # 导入中转区，提交成功后清空
├── txt-cache/             # TXT 转码产物（UTF-8）
│   └── {uuid}.txt
├── chapters/              # TXT 章节索引
│   └── {uuid}.json
├── locations/             # EPUB 位置索引
│   └── {uuid}.json
└── covers/                # 提取出的封面图
    └── {uuid}.{ext}
```

导入时**复制**文件到 `books/` 而非引用原路径，避免用户移动/删除源文件导致书籍失效。

三处设计说明：

**位置索引与章节索引独立成文件。** 长篇小说生成的 `locations` 是几百 KB 的 CFI 数组。把它塞进 `library.json` 会让「内存持有全量 + 防抖全量重写」（§4.3）在每次翻页落盘时都重写这个大块。`chapters/*.json` 同理。`library.json` 只保留轻量的书架元数据。

**TXT 保留原始字节 + 独立转码产物。** 编码检测有可能出错（见 §8）。保留原始文件使得检测错误时用户可以手动指定编码重新转码；转码产物是纯派生物，损坏时直接删掉重建即可。

**派生文件的命名是约定，不存进数据模型。** `txt-cache/{id}.txt`、`chapters/{id}.json`、`locations/{id}.json` 三者的文件名都可由 `id` 推导，不写入 `Book`，避免出现"字段值与磁盘实际状态不一致"的第二个事实来源。只有封面例外——它的扩展名取决于书内嵌图片的实际格式，因此 `coverFileName` 显式存储。

### 4.2 数据模型

```ts
type BookFormat = 'epub' | 'txt'

interface ReadingProgress {
  /** EPUB 用 CFI 字符串；TXT 用字符偏移量 */
  location: string | number
  /** 0 ~ 1 */
  percentage: number
  /** 百分比是精确值还是按章节序号估算，见 §5.5 */
  percentageExact: boolean
  chapterIndex: number
  chapterTitle: string
}

interface Book {
  id: string                 // uuid
  title: string
  author: string
  format: BookFormat
  fileName: string           // books/ 下的文件名
  coverFileName: string | null
  size: number               // bytes
  addedAt: number
  lastReadAt: number | null
  progress: ReadingProgress | null
  /** TXT 原始编码，用于诊断与重新转码；EPUB 为 null */
  sourceEncoding: string | null
  /** EPUB 位置索引是否已生成；TXT 恒为 true */
  locationsReady: boolean
}

interface ChapterIndex {
  title: string
  /** 该章起始字符偏移（含） */
  start: number
  /** 该章结束字符偏移（不含） */
  end: number
}

/** chapters/{id}.json 的内容 */
interface ChapterFile {
  /** 全书总字符数，同时用于计算 TXT 阅读进度 */
  totalCharacters: number
  chapters: ChapterIndex[]
}

interface ReaderSettings {
  fontSize: number                                  // px
  lineHeight: number                                // 倍数
  pageMargin: 'narrow' | 'medium' | 'wide'          // 见 §5.3
}

interface LibraryData {
  version: 1                 // 数据结构版本，供未来迁移
  books: Book[]
  settings: ReaderSettings
}
```

`ReadingProgress.percentageExact` 的存在是为了把 §5.2 说的"位置索引未就绪时进度条降级"落到可见行为上——估算时 UI 显示 `~35%`，索引生成完立刻切到精确值。字段定义见 §5.5。

`ReaderSettings.pageMargin` 用三档枚举而非裸数字，因为 §10 已确认这项对齐微信读书的「页边距」，只提供窄 / 适中 / 宽三档。枚举避免了出现无意义的中间值，也让持久化数据的取值范围有界。

### 4.3 为什么 MVP 用 JSON 而不是 SQLite

书架数据量级是几十到几百本书，`library.json` 全量读写耗时在毫秒级，完全够用。引入 SQLite 会带来 native 模块编译负担，收益为零。

**但要做隔离**：所有持久化访问收敛到 `main/services/repository.ts`，对外暴露的是 `listBooks()` / `upsertBook()` / `getSettings()` 这样的接口，不泄漏"底层是 JSON"这个事实。等到后续迭代引入书签、笔记、高亮（数据条数会到万级、且需要按书检索）时，只替换 repository 的实现，上层不受影响。

写入策略：内存持有全量数据，变更后**防抖 500ms** 落盘；应用退出前强制 flush 一次。

---

## 5. 核心技术方案

### 5.1 书籍文件如何到达渲染进程

epub.js 需要一个 URL 或 ArrayBuffer 来加载书。渲染进程不能直接读文件，因此**注册自定义协议**，挂载 appData 根目录并施加目录白名单：

```ts
// 应用启动前
protocol.registerSchemesAsPrivileged([
  {
    scheme: 'app',
    privileges: {
      standard: true,
      secure: true,
      supportFetchAPI: true,
      stream: true,
      // 缺这一项，渲染进程的 fetch('app://...') 会被 Chromium 直接拒掉：
      // "Cross origin requests are only supported for protocol schemes:
      //  chrome, chrome-extension, chrome-untrusted, data, http, https"
      // epub.js 内部走 XHR，同样会被拦，书完全加载不出来。（M1 实测）
      corsEnabled: true,
    },
  },
])

const ALLOWED_DIRS = ['books', 'txt-cache', 'chapters', 'covers', 'locations']

// app ready 后
protocol.handle('app', (request) => {
  const rel = decodeURIComponent(new URL(request.url).pathname).replace(/^\/+/, '')
  const top = rel.split('/')[0]
  if (!ALLOWED_DIRS.includes(top)) return new Response(null, { status: 403 })

  const abs = path.resolve(APPDATA_DIR, rel)
  // 穿越防护：规范化之后必须仍在对应白名单目录内
  if (!abs.startsWith(path.join(APPDATA_DIR, top) + path.sep)) {
    return new Response(null, { status: 403 })
  }
  return net.fetch(pathToFileURL(abs).toString())
})
```

渲染进程即可：

```ts
const book = ePub('app://local/books/xxx.epub')
const text = await fetch('app://local/txt-cache/xxx.txt').then(r => r.text())
```

选这个方案而不是「主进程读成 Buffer 再经 IPC 传给渲染进程」，理由是**不污染 IPC**——几百 MB 的二进制走 IPC 序列化既不优雅也慢。

> **一处被实测推翻的论据。** 本节初版还写了「支持 Range 请求（`stream: true`），大 EPUB 内部有图片 epub.js 会按需读取」。M1 实测证明这是错的：`app://` 的 Range 请求返回 200 和整个文件，不返回 206。
>
> 但这不影响结论，因为**前提本身就不成立**：EPUB 内部的图片在 zip 包里，epub.js 是把整个 zip 读进内存后从压缩包内取出图片并转成 blob URL 的（实测确认 `img src="blob:..."`），从来不按 HTTP 逐张请求图片。既然如此，Range 对这个场景没有意义，`stream: true` 保留即可、不必依赖。

> 白名单校验里 `top` 取自请求路径、`abs` 取自 `path.resolve` 的结果，两者必须都通过。只校验 `top` 挡不住 `books/../../library.json` 这类构造，只校验 `abs` 前缀则会把 `books-evil/` 这种同前缀目录放进来。

### 5.2 EPUB 分页渲染

```ts
const rendition = book.renderTo(containerEl, {
  flow: 'paginated',      // 分页而非滚动 —— 产品的核心交互
  spread: 'none',         // 单栏，因为「页边距」需要可调
  width: '100%',
  height: '100%',
})
rendition.display(savedCfi ?? undefined)
```

| 需求 | 实现 |
|---|---|
| 上/下页 | `rendition.prev()` / `rendition.next()` |
| 键盘翻页 | 监听 `keydown`：← / PageUp / ↑ 上一页，→ / PageDown / ↓ / 空格 下一页 |
| 点击翻页 | 容器左右三分之一区域设热区 |
| 当前位置 | `rendition.on('relocated', loc => ...)` |
| 全书进度 | `relocated` 载荷里的 `loc.start.percentage`，**不要**用 `locations.percentageFromCfi(cfi)`，理由见下 |
| 目录 | `book.loaded.navigation.toc` → `rendition.display(href)` |
| 恢复位置 | `rendition.display(savedCfi)` |

**必须处理的坑：**

1. **`book.locations.generate()` 是耗时操作。** 它要对全书做分词以计算位置索引，长篇小说可能需要数秒。做法：**首屏不阻塞**——打开书时若 `locationsReady` 为 false，立刻用「按 spine 章节序号估算」渲染进度条（`percentageExact: false`，UI 显示 `~35%`），同时后台异步生成；生成完成后写入 `locations/{id}.json`、把 `locationsReady` 置 true，进度条切成精确值。索引结果**不进 `library.json`**（见 §4.1）。

2. **改设置会丢位置。** 调整字号/行距后，原来那一页的内容会重排，`rendition` 内部位置会漂移。正确流程是：**改之前记录当前 CFI → 应用新样式 → `rendition.display(cfi)` 恢复到同一处**。少了这一步，用户每调一次字号就会"跳页"。这段逻辑不在调用方实现，收敛进 `ReaderEngine.applySettings`（§5.7）。

3. **图片、表格的跨页表现**是 epub.js 分页模式最常见的边界问题。需要专门的测试书目覆盖（含大图、宽表格、代码块的 EPUB），这是本项目最大的技术不确定性之一。M1 阶段用一个临时页面先摸一遍（§7）。

> **一处被实测推翻的 API 选择。** 本节原本写「全书进度 = `book.locations.percentageFromCfi(loc.start.cfi)`」。M3 实测发现它给出的值离谱：书的第一页算出 **50%**。
>
> 根因是 epub.js 的 `locationFromCfi` 拿 CFI 做**字符串比较**（`if (cfi < this._locations[i])`），而 `relocated` 给的是**点 CFI**（`epubcfi(/6/2!/4/2[pg-header]/2[...]/1:0)`）、位置索引里存的是**带逗号的区间 CFI**（`epubcfi(/6/2!/4,/2[pg-header]/2[...]/1:0,/4/4/2/30/1:0)`）。两者在第 7 个字符上一个是 `/`(0x2F)、一个是 `,`(0x2C)，比较结果一路跑偏。
>
> 正确做法是用 `relocated` 载荷里 epub.js 自己算好的 `loc.start.percentage` —— 实测同一页给出 0，正确。

4. **表格兜底要包一层可横向滚动的容器。** 实测宽表格在分页模式下横向溢出页面（15 列约 1.9 倍、源书自带的 204 单元格大表约 9.8 倍），直接用会被裁掉。实现在 `rendition.hooks.content` 里给每张表包一层 `overflow-x: auto` 的 div —— 注意这个 hook **每个章节渲染时才触发一次**，所以表格在第几章，就要翻到那一章才能看到效果。

### 5.3 阅读设置生效方式

字号、行距通过 epub.js 的主题系统直接注入，不修改原书 CSS：

```ts
rendition.themes.fontSize(`${settings.fontSize}px`)
rendition.themes.override('line-height', String(settings.lineHeight))
```

「页边距」的语义**对齐微信读书的「页边距」**：内容区居中，调整左右内边距，视觉结果是每行字数变化。三档：

```ts
const PAGE_MARGIN_PX = { narrow: 24, medium: 56, wide: 96 }

const px = PAGE_MARGIN_PX[settings.pageMargin]
rendition.themes.override('padding-left',  `${px}px`)
rendition.themes.override('padding-right', `${px}px`)
rendition.resize()   // 触发重新分页
```

单栏居中布局下，「改列宽」与「改页边距」是等价的（列变窄即留白变宽），因此实现上就是给内容容器加左右 padding。

> 与字号、行距一样，`resize()` 会导致位置漂移，必须走 §5.2 的「存位置 → 应用 → 恢复」。三档间距变化幅度大，这一步不能省。同样收敛进 `applySettings`。

### 5.4 TXT 处理

TXT 没有 CSS，需要自己造排版。分三步：

**（1）编码检测与转换**

中文 TXT 大量是 GBK / GB18030，直接按 UTF-8 读会乱码。主进程读文件为 Buffer 后用 `jschardet` 探测编码，再经 `iconv-lite` 转成 UTF-8。

产物写入 `txt-cache/{id}.txt`；`books/{id}.txt` 保留**原始字节**，检测到的编码记入 `Book.sourceEncoding`。这样当 `jschardet` 判断错误时，用户可以手动指定编码重新转码，而不必重新导入。

编码检测与转码只在导入时执行一次，后续打开直接读缓存。

**（2）章节切分**

切分同样在主进程做——文本已经在内存里，是纯函数计算，没必要绕一圈 IPC 把几十 MB 字符串传给渲染进程。

按正则匹配章节标题行。**三条正则都必须要求独占一行**：

```ts
const CHAPTER_PATTERNS = [
  // 中文数字章节
  /^[ \t]*(第[一二三四五六七八九十百千零〇0-9]{1,8}[章节回卷篇][^\n]{0,30})[ \t]*$/m,
  // Chapter N
  /^[ \t]*(Chapter\s+[0-9IVXLC]{1,6}[^\n]{0,40})[ \t]*$/im,
  // 纯数字编号：1. / 1、/ 001
  /^[ \t]*([0-9]{1,4}[.、][^\n]{0,30})[ \t]*$/m,
]
```

行首 `^` 与行尾 `$` 锚点是必须的。原文的 `第[…]+[章节回卷篇]` 没有锚点，"他第三章还没读完，因为……"这样的正文会被误判成章节标题。长度上限（30–40 字符）是第二道防线。

**降级判定必须量化。** 只写"匹配不到任何章节时降级"是不够的——误匹配出 2 个"章节"和正常切出 800 章是两种完全不同的情况。三条启发式，任一不满足即降级为按固定字数（5000 字）等分：

| 规则 | 阈值 | 挡住什么 |
|---|---|---|
| 章节数下限 | ≥ 3 | 正文里恰好出现两次"第三章"这类巧合 |
| 章节数上限 | ≤ 1000 | 把每一行都当标题的过度匹配 |
| 平均章节字数 | ≥ 500 | 标题行过短导致内容被切碎 |

上限取 1000 章 × 5000 字 = 500 万字，对长篇连载也足够宽松。

切分结果写入 `chapters/{id}.json`：每章的标题与字符起止偏移，以及**全书总字符数**。总字符数同时用于计算阅读进度百分比（§5.5），不需要额外扫描全文。

匹配不到任何章节时，降级方案同样产出一份 `chapters/{id}.json`，保证目录和进度条始终可用。

**（3）分页渲染**

不要逐段测量高度累加（慢且易错）。方案是用 **CSS 多列布局**让 Chromium 自己分页：把内容容器的宽高各固定为一页，配合 `column-fill: auto`，让纵向溢出的内容自动流到右边的列；外层视口 `overflow: hidden`，横向平移内容容器即完成翻页。

要让这个方案成立，CSS 必须同时满足两个约束：

1. **`column-width` 必须恰好等于容器的内容宽度。** 它是"理想宽度"而非精确宽度——容器宽度不整除时浏览器会拉伸列宽填满，导致「页边距」调节失真。
2. **固定宽 + 固定高 + `column-fill: auto` 三者缺一不可。** 多列布局的列数本身是横向算出来的，要得到"内容纵向溢出到右边的列"这个行为，依赖 Chromium 对这组属性的具体处理。

**M1 已实测确认这套方案成立**（见 §7.1）。实测数据：600px 宽的容器配 `column-width: 600px` + `column-gap: 40px`、`column-fill: auto`，一段长中文被分成了 14 列（`scrollWidth` 8920px），`translateX(-640px)` 精确平移一页宽度加一个列间距。**不需要走退路。**

确定可用的写法：

```css
.txt-columns {
  width: <每页宽>px;          /* 必须与 column-width 相等，否则浏览器会拉伸列宽填满，页边距调节失真 */
  height: <每页高>px;
  column-width: <每页宽>px;
  column-gap: 40px;
  column-fill: auto;
}
```

翻页 = 外层视口 `overflow: hidden` + 内容容器 `translateX(-(页宽 + 列间距) * 页码)`。

**退路方案**：若实测不成立，改用 `Range.getClientRects()` 二分查找每页的字符边界，得到 `pageBreaks: number[]`，每页只渲染自己那一段。这个方案确定可行，代价是每次重排都要重算——可在章节加载后后台算好并缓存。两条路对外的接口完全一致（§5.7），因此退路不会波及上层。

**大文件性能**：策略不是"少加载字符串"，而是**少挂载 DOM**。

渲染进程一次性 `fetch` 转码后的全文（`res.text()`），DOM 里**只放当前章**。内存里留一个几十 MB 的 JS 字符串不是瓶颈——真正的瓶颈是把全书几十万个 DOM 节点塞进页面。

> **初版这里写的是「挂载当前章节 ±1 章」，M4 实现时推翻了。** 多列布局的分页是按**整个内容容器**算的，三章塞进同一个容器会让「本章最后一页」的判断失效（列数是三章一起算出来的）。TXT 的章节切换只是一次 `replaceChildren`，几毫秒，没有预挂载相邻章的必要。±1 是照搬 EPUB 预加载的思路，对 TXT 不成立。

### 5.5 进度持久化时机

`relocated` 事件在翻页时高频触发，不能每次都写盘。

- 内存中实时更新 Zustand store（进度条要跟手）
- 落盘做 **1 秒防抖**
- 触发立即落盘的时机：切换章节、退出阅读页、窗口关闭（`before-quit` 中同步 flush）

**百分比的精确定义：**

| 场景 | 计算方式 | `percentageExact` |
|---|---|---|
| EPUB 且 `locationsReady` | `book.locations.percentageFromCfi(cfi)` | `true` |
| EPUB 且索引未就绪 | `(spineIndex + 1) / spineLength` | `false` |
| TXT | `字符偏移 / chapters/{id}.json 的 totalCharacters` | `true` |

`percentageExact` 为 false 时 UI 在百分比前加 `~`（`~35%`）。索引生成完成后立即重算并刷新，用户看到的是估算值"收紧"成精确值，而不是进度条莫名其妙跳一下。

书库页遵循同一规则：`progress` 为 null 时显示"未读"，估算值同样带 `~` 前缀。

### 5.6 导入流程

原文的流程在「文件复制进 `books/`」与「元数据解析完成」之间有一个窗口：如果 EPUB 畸形导致解析失败，磁盘上会永久留一个孤儿文件。改为 **staging + 原子提交**：

```
渲染进程                                   主进程
   │ api.importBook()
   │ ───────────────────────────────────────▶
   │                                   打开文件选择对话框
   │                                   校验扩展名 (.epub / .txt)
   │                                   生成 uuid
   │                                   复制 → books/.staging/{id}.{ext}
   │
   │                                   [TXT] 读原始字节 → jschardet
   │                                        → iconv-lite 转 UTF-8
   │                                        → 写 .staging/{id}.utf8.txt
   │                                        → 章节切分
   │                                        → 写 .staging/{id}.chapters.json
   │
   │                                   写 .staging/{id}.staged.json
   │                                     { originalName, format, sourceEncoding }
   │  ◀─────────────────────────────────────  { id, format, originalName, sourceEncoding }
   │
   ├─ EPUB: ePub('app://local/books/.staging/{id}.epub')
   │        → book.loaded.metadata → title / author
   │        → book.coverUrl() → blob → base64（带超时）
   │
   │ api.commitImport(id, { title, author, coverBase64, coverExt })
   │ ───────────────────────────────────────▶
   │                                   读 .staging/{id}.staged.json 得 format / 原名 / 编码
   │                                   .staging/{id}.{ext}   → books/{id}.{ext}
   │                                   [TXT] .utf8.txt       → txt-cache/{id}.txt
   │                                   [TXT] .chapters.json  → chapters/{id}.json
   │                                   [EPUB] 封面（如有）   → covers/{id}.{ext}
   │                                   → 写入 library.json
   ▼
刷书架列表
```

**渲染进程只回传它独有的东西。** 载荷里只有 `title / author / cover` —— 这三样是 epub.js 才能解析出来的。格式、原始文件名、原始编码、章节索引都由主进程在 import 阶段就掌握了，让它们绕一圈回到主进程是无谓的 IPC 往返，也让接口泄漏了主进程的实现细节。

> **本节曾有一处自相矛盾，已修正。** 早先的流程图一边画着主进程「章节切分，结果暂存内存」，一边又让渲染进程在 `commitImport` 的载荷里回传 `chapters?` —— 这两件事不可能同时成立。`chapters?` 是更早版本（渲染进程切分）留下的残留，现已删除。章节索引全程留在主进程，经 staging 中转（见下方"为什么经 staging 而不是内存"）。

**`staged.json` 为什么必须存在。** commit 阶段要用原始文件名做书名兜底（§11.1 的降级规则），但那时手上的文件名已经是 `{id}.epub` 这种，读不出原名了。原始编码同理。这些信息在 import 阶段是现成的，落一个小文件存下来即可。

**失败路径**：任何一步出错，渲染进程调 `api.abortImport(id)` 让主进程清理；即便渲染进程崩溃、或用户直接关掉应用，`.staging/` 里的残留也会在**下次启动时被清空**。

**`abort` 按前缀清扫，不写死文件名。** staging 里的产物种类会随功能增加（现在已经四种：`{id}.{ext}`、`{id}.utf8.txt`、`{id}.chapters.json`、`{id}.staged.json`），写死文件名意味着每加一种就得改一次 `abort`，漏改就是磁盘泄漏。实现上扫 `.staging/` 删掉所有以 `{id}.` 开头的条目即可。

**为什么经 staging 而不是主进程内存。** 内存方案（流程图最初写的"暂存内存"）也能跑通，但会让 `abort` 需要同时维护「文件」和「内存表」两份状态——加一种 staging 产物时很容易只记得改文件那边。统一落到文件之后，`abort` 与启动清理共用同一个「扫目录」机制，没有第二份状态可漂移。

**第 2 步的 EPUB 解析必须自带超时。** §7.1 实测发现：用截断的 OPF 样本时，epub.js 的 `openFailed` 事件正常触发，但 `rendition.display()` **既不 resolve 也不 reject，会一直挂着**。只靠捕获异常来回滚是不够的——必须给解析加一个超时，超时后走同一条 `abortImport` 路径，否则导入会永远卡住、`.staging/` 里的残留也不会被清掉。

由此，`books/` 与 `library.json` 中永远只存在完整提交过的书，不存在"半本书"状态。

### 5.7 阅读引擎接口

`EpubEngine` 与 `TxtEngine` 实现同一套接口，上层 Reader 页面不感知格式差异：

```ts
interface ReaderEngine {
  /** 加载书籍并恢复到指定位置；location 为 null 时从头开始 */
  load(book: Book, location: string | number | null): Promise<void>
  prev(): void
  next(): void
  goToChapter(index: number): void
  goToLocation(location: string | number): void
  /** 内部负责「记录当前位置 → 应用新样式 → 恢复到同一处」 */
  applySettings(settings: ReaderSettings): void
  onRelocated(cb: (progress: ReadingProgress) => void): () => void
  destroy(): void
}
```

把 `applySettings` 收进接口是这里最有价值的一点：§5.2 的坑 2 与 §5.3 那条"三档间距变化幅度大，这一步不能省"其实是同一个问题，EPUB 与 TXT 两侧都要踩。关在各自实现里，这段逻辑只写一次，上层与测试都更干净。

`onRelocated` 返回取消订阅函数，供 React 组件在卸载时清理。

> **实施时的切分**：`applySettings` 依赖阅读设置，那是 M5 的活。M3 的 `EpubEngine` 只实现用得上的方法 —— 上面这些去掉 `applySettings`，再加上 M3 需要的三个：`goToPercentage(percentage)`（拖拽跳转）、`resize()`（窗口尺寸变化后重新分页）、`locationsReady()`。不交付一个里面是空壳的方法。M5 再把 `applySettings` 加进接口。另外 `onRelocated` 在 M3 的形态是构造时传入的 `callbacks.onRelocated`，不是订阅式的返回取消函数——单次挂载的阅读页不需要动态增删订阅。

### 5.8 删除书籍的清理范围

删除一本书需要连带清理五类文件：

| 文件 | 适用格式 |
|---|---|
| `books/{id}.{ext}` | 全部 |
| `txt-cache/{id}.txt` | TXT |
| `chapters/{id}.json` | TXT |
| `locations/{id}.json` | EPUB（若已生成） |
| `covers/{id}.{ext}` | 有封面时 |

**顺序：先从 `library.json` 移除记录并落盘，再删文件。** 反过来的话，文件删了但写盘失败，会留下一本点不开的僵尸书。文件删除失败（Windows 上被占用）只记日志不中断——书架已经不显示它了。

---

## 6. 测试策略

> 项目规范要求：每次改动后必须编写或更新测试，交付前全部通过。

### 6.1 单元测试（Vitest）

优先覆盖纯函数，这是性价比最高的部分：

| 模块 | 用例 |
|---|---|
| `main/services/txt/chapters.ts` | 各种章节标题格式的切分；**正文中含"第三章"字样时不被误判**；三条降级启发式的边界；空文件 |
| `main/services/txt/encoding.ts` | UTF-8 / GBK / GB18030 样本文件的正确解码 |
| `lib/progress.ts` | 百分比计算（含 `percentageExact` 两种取值）、防抖落盘的行为 |
| `main/services/repository.ts` | 增删改查、并发写入、损坏 JSON 的容错、版本迁移、**索引文件不写入 library.json** |
| `main/services/bookFiles.ts` | **路径穿越防护**（`../../` 必须被拒绝）；白名单之外的前缀（如 `books-evil/`）必须被拒绝；staging 提交与回滚 |

### 6.2 组件测试（Vitest + RTL）

书架列表渲染、设置面板交互、目录面板跳转回调。

### 6.3 端到端测试（Playwright + Electron）

覆盖 MVP 主链路，按优先级分两档。**已实现**（M7），整套 6 条 13.3 秒：

| # | 场景 | 优先级 | 落在哪 |
|---|---|---|---|
| 1 | 导入一本 EPUB → 书架出现该书 → 封面/书名/作者正确 | **P0** | `tests/e2e/p0.spec.ts` |
| 2 | 打开 → 翻三页 → 退出 → 重新打开 → **位置恢复** | **P0** | `tests/e2e/p0.spec.ts` |
| 3 | 导入 TXT（GBK 编码样本）→ 打开 → 中文不乱码 | P1 | `tests/e2e/p1.spec.ts` |
| 4 | 调整字号 → 位置不跳变 | P1 | `tests/e2e/p1.spec.ts` |
| 5 | 删除书籍 → 书架移除，`books/` 下文件已清除 | P1 | `tests/e2e/p1.spec.ts` |

P0 覆盖「导入」与「位置恢复」——前者是数据正确性，后者是本项目最核心的体验承诺。其余三条价值同样高但触发频率低，放在手动档执行（见 §6.5）。

**两个关键做法**（都在 `tests/e2e/helpers/app.ts`）：

- **数据隔离靠 `--user-data-dir`**，测试跑在临时目录里，不碰用户的真实书架
- **原生对话框用 `electronApp.evaluate` 在主进程里替换**。Playwright 点不了原生对话框，但能在主进程里执行代码，直接换掉 `dialog.showOpenDialog` 与 `showMessageBox` 即可

因此**没有为可测性往生产代码里加任何测试钩子**。Playwright 驱动 Electron 用的是应用自带的 Chromium，也不需要下载浏览器二进制。

> **跑的是构建产物而不是打包产物**（§6.5 的档位表）。`npm run build` + `electron .` 已经是 `file://` 加载的生产模式 —— M6 查出的那个竞态就是这么发现的；打包产物要求先跑一次两分多钟的 electron-builder，放日常反馈里不划算。

### 6.4 必须人工验证的部分

epub.js 的分页是**视觉行为**，E2E 断言难以覆盖。以下需要人工书目清单验证，样本放在 `tests/fixtures/`：

- 大量内嵌图片
- 超宽表格（必然溢出单页）
- 代码块、引用块
- 中英混排
- 无封面 / 无作者元数据
- 损坏的 OPF（容错性）

**样本来源与落盘方式**：样本 EPUB 由一次性脚本程序化生成，产物**以二进制形式提交进仓库**，脚本一并提交留档但不在 CI 中执行——这样 CI 既不需要 zip 写入依赖，也不需要联网。

另从 Standard Ebooks 取 1–2 本 EPUB 3 规范排版的公有领域书作为真实样本，**在 M3 阶段下载一次后同样提交进仓库**，此后 CI 全程离线。

TXT 样本手工构造，GBK / GB18030 / UTF-8 各一份几 KB 的小文件。

### 6.5 CI 与自动化执行

主仓库托管在 **GitHub**，CI 使用 **GitHub Actions**。若后续国内拉取变慢，再补一个 Gitee 镜像仓库，但不作为主仓库。

**本地脚本**（CI 与本地共用同一入口，保证行为一致）：

```json
// package.json
"scripts": {
  "lint":      "eslint .",
  "typecheck": "tsc --noEmit",
  "test":      "vitest run",
  "test:e2e":  "playwright test",
  "verify":    "npm run lint && npm run typecheck && npm run test"
}
```

- **`npm run verify`** 是「交付前验证」的单一入口，覆盖静态检查 + 单元/组件测试。
- **`npm run test:e2e`** 单独执行。E2E 需要启动 Electron，耗时较长。

**工作流**（`.github/workflows/ci.yml`）：

```yaml
runs-on: windows-latest     # 本项目是 Windows 桌面应用，
                            # Linux runner 验证不了打包与路径行为
steps:
  - checkout
  - setup-node 24 (cache: npm)
  - npm ci
  - npm run verify
```

触发策略：

| 事件 | 执行内容 |
|---|---|
| `pull_request` | `npm run verify` |
| `push` 到 `main` | `npm run verify` + E2E **P0 两条**（§6.3） |
| `workflow_dispatch` | 完整套：`verify` + 全部 5 条 E2E + 打包产物验证 |

**本地与 CI 共用同一入口**：`npm run test:e2e` 自带 `npm run build`，避免出现「CI 上跑的和本地跑的不是一回事」。E2E 失败的 job 会把 `test-results/` 传成 artifact 便于事后翻现场。

日常迭代不因 E2E 拖慢反馈，关键节点仍有覆盖。

**Windows runner 上启动 Electron 的两点注意：**

- `windows-latest` 有完整桌面会话，**不需要 xvfb**
- 需要给 Electron 传 `--disable-gpu`，否则会出现显卡相关的偶发超时

**两条必须入库的工程配置**（M1 实测踩到，漏了会在 CI 上直接失败）：

- **`package.json` 的 `allowScripts`。** npm 11 起默认拦截依赖的 postinstall 脚本，Electron 的二进制因此不会下载。必须用 `npm approve-scripts electron` 把授权写进 `package.json`，否则 CI 上 `npm ci` 后没有任何可执行的 Electron。
- **`.npmrc` 里的 `ELECTRON_MIRROR`。** Electron 二进制从 GitHub Release 下载，国内实测 15 分钟无进展。指向 `https://npmmirror.com/mirrors/electron/` 可解。注意这条也作用于 CI —— GitHub runner 访问 npmmirror 正常，若镜像不可用删掉该文件即可回到直连。

> **Node 版本说明**：本节原写 Node 20，但 Node 20 已于 2026-04 EOL（不再有安全补丁），且本机开发环境是 24。CI 与本地都取 24，避免版本漂移导致的「本地过、CI 挂」。Node 24 自 2025-10 起为 LTS。

> **额度提醒**：私人仓库免费额度 2000 分钟/月，Windows runner 按 **2 倍倍率**计费，实际约 1000 分钟。E2E 单次启动 Electron 约 1–3 分钟，因此只把 P0 两条放在 push 到 main，其余场景留给手动触发。

**pre-push 钩子不再需要**。引入 CI 后，钩子会拖慢每次推送，且「交付前验证」的触发点是我完成改动时（见 CLAUDE.md），而非推送时 —— 由我在交付前执行 `npm run verify` 即可。

---

## 7. 实施路径

> **当前进展与续接点见 [docs/进度.md](./docs/进度.md)。** 本节只定义路径，不记录状态。

| 阶段 | 内容 | 产出 |
|---|---|---|
| **M1 骨架 + 风险验证** ✅ | electron-vite 工程搭建、IPC 通道与类型定义、安全基线与 CSP、`app://` 协议、数据层三个服务 + 48 个用例；三项风险验证 | 可启动的窗口；数据层有测试覆盖；**三项验证结论已写入 §7.1，最大风险已释放** |
| **M2 书架** ✅ | 导入流程（staging / 提交 / 回滚）、EPUB 元数据与封面提取、书架列表、删除；**TXT 的编码检测与章节切分也在此落地** | 能导入并展示书，重启后仍在；130 个用例 |
| **M3 EPUB 阅读** ✅ | 分页渲染、翻页、目录、进度条（可拖拽）、位置恢复；`ReaderEngine` 接口与 `EpubEngine`；位置索引持久化 | 主链路打通；175 个用例 |
| **M4 TXT 阅读** ✅ | 多列分页、跨章翻页（编码检测与章节切分已在 M2 完成） | 两种格式均可用；192 个用例 |
| **M5 设置与打磨** ✅ | 字号 / 行距 / 页边距（离散档位）、位置保持、异常态 | MVP 功能完整；220 个用例 |
| **M6 打包** ✅ | electron-builder 配置、NSIS 安装包、免安装版、图标 | 可交付的 Windows 安装包（`release/` 下两个 ~110MB 的 .exe） |

### 7.1 M1 的三项风险验证（已完成）

初版把最大风险的释放放在 M3，但 M3 排在 M2 之后、且依赖 M2 的书架，时机过晚。因此把验证提到 **M1 末尾**，用一个临时页面跑掉，不做成正式功能。

**三项验证均已执行完毕，结论如下。这是 M2 开工的前提。**

---

#### 验证 1：TXT 多列分页的 CSS 组合 —— ✅ 成立

用 600×400 的视口、`column-width: 600px`、`column-gap: 40px`、`column-fill: auto` 渲染一段长中文：

| 指标 | 实测值 |
|---|---|
| 内容容器 `scrollWidth` | 8920px（视口宽 602px） |
| 生成列数 | 14 |
| `translateX(-640px)` 后的位移 | 640px（= 页宽 600 + 列间距 40，精确） |

**结论**：Chromium 确实会把纵向溢出的内容排到右侧的新列，`translateX` 可按页平移。§5.4 的方案成立，**不需要走 `Range.getClientRects()` 二分分页的退路**。确定可用的 CSS 已写入 §5.4。

---

#### 验证 2：epub.js 在受限 CSP 下渲染 —— ✅ 成立（但需要三处修正）

用真实公有领域书（Project Gutenberg 的 *Pride and Prejudice*，16 个 spine 章节）+ 五个由脚本生成的变体样本验证。

**功能全部正常**：拿到 `title` / `creator` 元数据、63 条目录、封面 blob URL、章节正文文本、书籍内图片、`next()` 翻页拿到 CFI。

**但最初完全跑不起来，实测暴露出三处必须修正的地方**，前两处已写入 §2.2，第三处写入 §5.1：

| 问题 | 现象 | 修正 |
|---|---|---|
| `app://` 未被标记为 CORS 可用 | 渲染进程 fetch 与 epub.js 的 XHR 全部被拒：`Cross origin requests are only supported for protocol schemes: chrome, chrome-extension, ...`。书完全加载不出来 | 特权标志补 `corsEnabled: true` |
| `style-src` 缺 `blob:` | epub.js 用 blob URL 挂载书籍样式表，被静默拦掉 | `style-src` 加 `blob:` |
| `base-uri 'none'` | epub.js 要靠设置 document base URI 解析书内相对路径，该操作被拦 | **移除 `base-uri` 指令** |

根因是 **epub.js 的书籍 iframe 会继承父页面的 CSP**，所以这些指令实际也约束着书籍内容，不能按普通网页的严格程度设置。

**表格边界问题的实测数据**（§5.2 的坑 3）：

| 样本 | 列数 | 单元格数 | 表格渲染宽度 | 页面宽度 | 溢出倍数 |
|---|---|---|---|---|---|
| 注入的超宽表格 | 15 | 105 | 1150px | 602px | ~1.9× |
| 源书自带的大表 | — | 204 | 5906px | 602px | ~9.8× |

**结论**：epub.js 会正常渲染表格，但**表格会横向溢出页面容器，且溢出量可能接近 10 倍**。这是 §8 里那条高风险项的实证，M3 必须准备 CSS 兜底（候选：包一层 `overflow-x: auto` 的容器，或对超宽表格做等比缩放）。

---

#### 验证 3：`app://` 是否支持 Range 请求 —— ❌ 不支持，但不影响方案

带 `Range: bytes=0-99` 请求，得到的是 `200` + 完整 558381 字节，而非 `206` + 100 字节，响应也没有 `Content-Range` 头。

**但这不构成风险，因为原本的担忧建立在一个错误的模型上。** 初版认为「大 EPUB 内部的图片要按需读取」，而实际上 EPUB 内部的图片在 zip 包里，epub.js 是把整个 zip 读进内存后从压缩包内取出图片并转成 blob URL 的（实测确认 `img src="blob:..."`），**从来不按 HTTP 逐张请求图片**。既然没有逐资源请求，Range 就无从发挥作用。

**结论**：不需要回退到「主进程读 buffer 经 IPC 传递」。§5.1 中关于 Range 的论据已删除并加注说明。

---

#### 顺带发现的一条 M2 注意事项

**损坏的 OPF 会让 epub.js 挂起而不是报错。** 用截断 OPF 的样本测试时，`book.on('openFailed')` 正常触发，但 `rendition.display()` **既不 resolve 也不 reject，一直挂着**（等满 20 秒超时才结束）。

因此 §5.6 的导入流程**必须自带超时**：不能只依赖 epub.js 抛错来回滚 staging，否则解析畸形 EPUB 时导入会永远卡住、`.staging/` 里的残留文件也不会被 `abortImport` 清掉。

---

## 8. 风险清单

| 风险 | 等级 | 状态与应对 |
|---|---|---|
| **表格在 epub.js 分页模式下横向溢出** | **高** | **已实证**（§7.1 验证 2）：15 列表溢出 1.9×，204 单元格大表溢出 9.8×。M3 必须做 CSS 兜底 |
| **epub.js 对损坏 EPUB 挂起而不报错** | **高** | **M1 新发现**（§7.1）：`openFailed` 会触发，但 `display()` 永不 settle。§5.6 导入流程**必须自带超时**，否则导入卡死且 `.staging/` 残留不清 |
| epub.js 分页在图片上的边界问题 | 中 | 待 M3 用 `many-images.epub`（200 张图）验证 |
| **epub.js 打开无封面书时抛未处理拒绝** | 低 | **M2 实测**：`TypeError: ... replaceCss` 与 `TypeError: ... navigation` 直接打在 renderer console 上。发生在 `book.open()` 阶段，早于本项目的任何代码，拦不住。**不影响功能**（导入照常成功、`coverFileName` 为 null），只是噪音会掩盖真实错误。等 M3 接入正式阅读流程时再评估是否需要在 `unhandledrejection` 上做过滤 |
| epubjs 停更导致 bug 无人修 | 中 | 锁定 0.3.93；必要时用 `patch-package` 打局部补丁 |
| `locations.generate()` 长书耗时数秒 | 中 | 后台异步 + 独立文件缓存 + `percentageExact` 标记的降级显示 |
| 改设置导致阅读位置跳变 | 中 | 收敛进 `ReaderEngine.applySettings`（§5.7），两种格式只写一次 |
| TXT 编码识别错误导致乱码 | 中 | 保留原始字节 + `sourceEncoding` 落库，支持手动指定编码重新转码 |
| TXT 章节标题被误判 | 中 | 行锚点 + 长度上限 + 三条降级启发式（§5.4） |
| 超大 TXT（>30MB）渲染卡顿 | 中 | 只挂载当前章节 ±1 的 DOM；不限制内存中的字符串 |
| 打包后 `app://` 协议路径解析差异 | 中 | 用 `path.resolve` 而非字符串拼接；打包后做一次完整回归 |
| 导入中途崩溃留下残留文件 | 低 | staging + 原子提交 + 启动时清空 `.staging/`（§5.6） |
| 用户移动源文件导致书籍失效 | 低 | 导入时复制副本到 `books/` |
| ~~TXT 多列分页的 CSS 行为不符合预期~~ | — | **M1 已排除**（§7.1 验证 1），不需要退路 |
| ~~epub.js 在受限 CSP 下渲染失败~~ | — | **M1 已解决**（§7.1 验证 2），修正三处 CSP / 协议配置 |
| ~~`app://` 不支持 Range 请求~~ | — | **M1 已排除**（§7.1 验证 3），该担忧的前提本身不成立 |

---

## 9. 依赖清单

**运行时**

```
electron
epubjs                 # 锁定 0.3.93，不加 ^
jschardet
iconv-lite
zustand
react / react-dom
```

**开发时**

```
electron-vite          # ^5
vite                   # 必须锁 ^7，见下方版本约束
@vitejs/plugin-react   # 必须锁 ^5，见下方版本约束
electron-builder       # M6 引入
typescript
tailwindcss            # M2 引入
@radix-ui/react-*      # 按需引入
vitest
@testing-library/react # M2 引入
@playwright/test       # M3 引入
eslint / prettier
patch-package          # 仅在需要对 epubjs 打补丁时引入
```

**版本约束（M1 实测）：`electron-vite@5` 的 peer 只支持 `vite ^5 || ^6 || ^7`，而最新的 `vite@8` 又被 `@vitejs/plugin-react@6` 要求 —— 两者互斥，npm 直接 ERESOLVE 报错。** 解法是锁 `vite@^7` + `@vitejs/plugin-react@^5`（5.2.0 兼容 vite 4.2 ~ 8）。等 `electron-vite@6` 正式版支持 vite 8 之后再放开。

依赖刻意保持精简。任何新增依赖前先问：标准库或已有依赖能否覆盖。

---

## 10. 已确认的产品决策

1. **「页边距」对齐微信读书** —— 内容区居中 + 左右留白三档调节，详见 §5.3。
2. **不做深色 / 护眼主题** —— MVP 不引入主题切换，阅读区固定浅色背景。若后续需要，EPUB 侧需在主题系统覆盖 `background` 与 `color`，TXT 侧成本很低。色值集中定义在一处（§11），为将来留出单点修改的可能。
3. **TXT 无规范章节标题时走降级方案** —— 按固定字数切分，接受目录质量下降。不做用户手动指定章节规则的功能。
4. **TXT 保留原始字节 + 独立转码产物** —— 支持编码误判后手动指定编码重新转码。
5. **EPUB 引擎锁定 epubjs 0.3.93** —— 接受其停更状态，需要时用 `patch-package` 局部修补（§1.3）。
6. **视觉基准参照微信读书** —— 见 §11。
7. **测试样本：脚本自造 + 公有领域真实书** —— 产物二进制入库，CI 全程离线（§6.4）。

---

## 11. 视觉基准

交互与视觉参照微信读书：克制、以内容为中心、无多余装饰。

### 11.1 书架页

- 网格布局，列数随窗口宽度响应，每行 4–8 个
- 卡片：封面（3:4）+ 书名（最多 2 行，溢出省略）+ 作者（1 行，溢出省略）
- 缺封面时用书名首字生成占位封面
- **进度做在封面右下角角标**：`35%` / 估算值 `~35%` / 未读不显示
- 顶部只有标题与「导入」按钮。MVP 只有"最近阅读"一种排序，因此**不做排序 UI**
- 删除入口放在卡片悬停态（或右键菜单）
- 空态：居中提示 + 导入按钮

### 11.2 阅读页

- 沉浸式无边框，内容居中
- 顶部中央显示当前章节名，点击呼出目录
- 底部细进度条 + 百分比，**可拖拽跳转**。拖动过程只更新预览、松手才真跳（连续 `display()` 会把渲染打爆）；位置索引未就绪时退化为按章节比例粗跳
- **点击热区**：左 1/3 上一页、右 1/3 下一页、**中间 1/3 切换控件显隐**
- 控件呼出时上下工具栏浮现（顶部：返回 / 章节名 / 设置；底部：进度条 + 页码）

### 11.3 面板

- **目录**：左侧抽屉滑出，当前章节高亮
- **设置**：底部滑出，三组——字号、行距、页边距（窄 / 适中 / 宽）

### 11.4 配色

产品文档 §10 已确认不做深色主题，因此**不建主题变量体系**，色值直接写死浅色。但集中定义在 `src/renderer/src/theme.ts` 与 Tailwind 配置中，将来真要加深色时改一处而不是全库搜索。

- 阅读区背景：浅色（`#F7F7F7` 或纯白）
- 正文：深灰（`#2B2B2B`），避免纯黑在高对比下的刺激
- 控件：低饱和灰阶
- 强调色：单一低饱和色，用于进度条与选中态
