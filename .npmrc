# Electron 的二进制从 GitHub Release 下载，国内会卡住（实测 15 分钟无进展）。
# 走 npmmirror 镜像。注意：这一条同样作用于 CI —— GitHub runner 访问
# npmmirror 正常，但若哪天镜像不可用，删掉本文件即可让 CI 回到 GitHub 直连。
ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
